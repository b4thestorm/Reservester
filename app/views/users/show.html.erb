 <h1>Hello,<%=current_user.username%></h1>

 <ul class="nav nav-tabs">
 	<li class="active">
 		<%=link_to 'Restaraunts (' + current_user.restaraunts.size.to_s + ')', user_path(@user)%>
 	</li>
 	<li>
 		<%=link_to 'Reservations ()', user_path(@user)%>
 	</li>
 </ul>

 <% current_user.restaraunts.each do |rest| %>
 <div class="well">

<%= rest.name%></br>
<%= rest.description%></br>
<%= rest.address%></br>
<%= rest.number%>
</div>
 <%end%>
